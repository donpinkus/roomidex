<nav class="navbar navbar-default navbar-fixed-top">
  <div class="navbar-header">
    <%= link_to "Roomidex", root_path, :class => 'navbar-brand' %>
    <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>

  </div>
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="
          <%= params[:controller] == "users" && params[:action] == "index" ? 
            "active" : nil %>" >
          <%= link_to raw('<i class="glyphicon glyphicon-search"></i> Search'), users_path %></a>
        </li>

        <li class="
          <%= params[:controller] == "roomidex_relationships" && params[:action] == "index" ? 
            "active" : nil %>" >
          <%= link_to raw('<i class="glyphicon glyphicon-list"></i> My Roomidex'), roomidex_relationships_path %></a>
        </li>

        <li class="
          <%= params[:controller] == "messages" && params[:action] == "index" ? 
            "active" : nil %>" >
          <%= link_to raw('<i class="glyphicon glyphicon-comment"></i>&nbsp; Messages'), messages_path %></a>
        </li>

        <li class="
          <%= params[:controller] == "users" && params[:action] == "show" && params[:id].to_i == current_user.id ? 
            "active" : nil %>" >
          <%= link_to raw('<i class="glyphicon glyphicon-user"></i> ') + current_user.name, user_path(current_user) %></a>
        </li>
        <li>
          <div class="navbar-form">
            <%= link_to 'Logout', signout_path, class: "btn btn-default btn-sm" %>
          </div>
        </li>
      <% else %>
        <li>
          <div class="navbar-form">
            <%= link_to 'Login', signin_path, class: "btn btn-primary" %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
<div class="navbar-spacer">
</div>
